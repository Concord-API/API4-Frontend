<script setup lang="ts">
import type { HTMLAttributes } from "vue"
import { cn } from '@/shared/lib/utils'

const props = defineProps<{
  class?: HTMLAttributes["class"]
}>()
</script>

<template>
  <div
    data-slot="card"
    :class="
      cn(
        'bg-card text-card-foreground flex min-w-0 flex-col gap-4 rounded-lg border p-4 shadow-sm transition-shadow sm:gap-5 sm:p-5',
        props.class,
      )
    "
  >
    <slot />
  </div>
</template>
